export interface Colums {
  description: string;
  id: number;
  title: string;
}

export interface Prayers {
  checked: boolean;
  columnId: number;
  description: string;
  id: number;
  title: string;
}
